LOAD DATA 
INFILE 'data_file.txt'
APPEND
	INTO TABLE staff
	FIELDS TERMINATED BY ','
	(STAFF_ID		,
	 STAFF_FIRST_NAME	,
	 STAFF_LAST_NAME	,
	 GENDER 		,
	 EMAIL_ID		,
	 DATE_OF_BIRTH		,
	 ROLE 			,
	 IMG1_FILE_NAME		FILLER	CHAR(100),
	 STAFF_IMAGE		LOBFILE(IMG1_FILE_NAME) TERMINATED BY EOF,
	 INSTRUCTOR_ID		,
	 PAY_RATE		)
	 